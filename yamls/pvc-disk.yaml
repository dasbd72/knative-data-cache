apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-volume-claim-disk
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  storageClassName: manual
